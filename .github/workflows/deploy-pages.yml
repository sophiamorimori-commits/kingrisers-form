name: Deploy to GitHub Pages

on:
  push:
    branches:
      - claude/app-development-5uepbe
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: vocab-app/package-lock.json

      - name: Install vocab-app dependencies
        working-directory: vocab-app
        run: npm ci

      - name: Build vocab-app
        working-directory: vocab-app
        run: npm run build

      - name: Assemble site
        run: |
          mkdir -p site
          cp index.html site/index.html
          mkdir -p site/vocab-app
          cp -r vocab-app/dist/. site/vocab-app/

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
